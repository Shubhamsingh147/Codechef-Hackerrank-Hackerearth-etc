#include <iostream>
#include <stdlib.h>
#include <math.h>
using namespace std;

int main()
{
	int t,n;
	cin>>t;
	while(t--)
	{
		long long int ans=0;
		cin>>n;
		long long p[i],a[i];
		for (int i = 0; i < n; ++i)
			cin>>p[i];
		for (int i = 0; i < n; ++i)
			cin>>a[i];
		for (int i = 0; i < n; ++i)
		{
			
		}